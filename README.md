# Browser Fingerprinting Utility

Simple browser fingerprinting tool used for collecting browser information and tracking visitors to websites with the tracker code.


## Getting Started
These command will build the executable and tracker will serve the neccessary web resources for the project.
```bash
make build
./tracker
```
